#pragma once

#include "defines.h"


// Adiciona o nome das extensões necessárias para a plataforma em names_darray. Que sera criado e passado
void platform_get_required_extension_names(const char*** names_darray);
